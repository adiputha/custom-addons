<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Kanban View -->
    <record id="view_kanban_float_request" model="ir.ui.view">
        <field name="name">float.request.kanban</field>
        <field name="model">float.request</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_horizontal_floats" create="false"
                edit="false"
                delete="false">
                <field name="id" />
                <field name="name" />
                <field name="department_id" />
                <field name="initial_amount" />
                <field name="current_amount" />
                <field name="cash_in_hand" />
                <field name="iou_amount" />
                <field name="float_manager_id" />
                <field name="total_petty_cash_requests" />
                <field name="total_iou_requests" />
                <field name="total_requests" />
                <field name="date_created" />
                <field name="state" />
                <field name="current_denomination_id" />
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card  oe_kanban_global_click oe_kanban_no_click">
                            <div class="card-header">
                                <div>
                                    <strong>
                                        <t t-esc="record.name.value" />
                                    </strong>
                                    <div style="font-size: 12px; color: #6c757d; margin-top: 2px;">
                                        <t t-esc="record.department_id.value" />
                                    </div>
                                </div>
                                <span>
                                    <strong>Rs. <t t-esc="record.initial_amount.value" /></strong>
                                </span>
                            </div>
                            <div>
                                <div class="float-card-buttons">
                                    <button type="object"
                                        name="action_view_petty_cash_requests"
                                        class="btn btn-primary"
                                        t-attf-context="{
                                    'search_default_float_request_id': {{record.id.raw_value}},
                                    'default_float_request_id': {{record.id.raw_value}},
                                }">
                                        Petty Cash Request
                                    </button>
                                    <button type="object" name="action_view_iou_requests"
                                        class="btn btn-primary"
                                        context="{
                                    'search_default_float_request_id': id,
                                    'default_float_request_id': id,
                                    }">
                                        IOU Request
                                    </button>
                                    <button class="btn btn-primary" type='object'
                                        name='action_request_reimbursement'>Reimbursement</button>
                                    <button class="btn btn-primary" type="object"
                                        name="action_view_denominations">Denomination
                                        Count</button>
                                </div>

                                <div class="float-card-summary">
                                    <p>
                                        <span>Cash in hand:</span>
                                        <span>Rs. <t t-esc="record.cash_in_hand.value" /></span>
                                    </p>
                                    <p>
                                        <span>IOU Amount:</span>
                                        <span>Rs. <t t-esc="record.iou_amount.value" /></span>
                                    </p>
                                    <p>
                                        <span>Total Requests:</span>
                                        <span>
                                            <t t-esc="record.total_requests.value" />
                                        </span>
                                    </p>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>


    <record id="view_tree_float_request" model="ir.ui.view">
        <field name="name">float.request.list</field>
        <field name="model">float.request</field>
        <field name="arch" type="xml">
            <list string="Petty Cash Floats">
                <field name="date_created" />
                <field name="name" />
                <field name="department_id" />
                <field name="float_manager_id" />
                <field name="initial_amount" widget="monetary" />
                <field name="current_amount" widget="monetary" />
                <field name="cash_in_hand" widget="monetary" />
                <field name="total_requests" />
                <field name="state" widget="badge"
                    decoration-success="state == 'approved'"
                    decoration-warning="state == 'requested'"
                    decoration-info="state == 'draft'"
                    decoration-danger="state == 'rejected'" />

            </list>

        </field>

    </record>


    <record id="view_form_float_request" model="ir.ui.view">
        <field name="name">float.request.form</field>
        <field name="model">float.request</field>
        <field name="arch" type="xml">
            <form string="Create New Float" editable="top">
                <header>
                    <button name="action_submit" string="Submit for Approval"
                        type="object" class="btn-primary"
                        invisible="state != 'draft'" />
                    <button name="action_approve" string="Approve"
                        type="object" class="btn-success"
                        invisible="state != 'requested'"
                        groups="account.group_account_manager" />
                    <button name="action_reject" string="Reject"
                        type="object" class="btn-danger"
                        invisible="state != 'requested'"
                        groups="account.group_account_manager" />
                    <button name="action_setup_denominations" string="Setup Denominations"
                        type="object" class="btn-info"
                        invisible="state != 'approved' or current_denomination_id"
                        help="Setup initial cash denominations for this float" />
                    <button name="action_reset_to_draft" string="Reset to Draft"
                        type="object" class="btn-secondary"
                        invisible="state != 'rejected'" />
                    <field name="state" widget="statusbar"
                        statusbar_visible="draft,requested,approved" />
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_petty_cash_requests" type="object"
                            class="oe_stat_button" icon="fa-money">
                            <field name="total_petty_cash_requests" widget="statinfo"
                                string="Petty Cash Requests" />
                        </button>
                        <button name="action_view_iou_requests" type="object"
                            class="oe_stat_button" icon="fa-handshake-o">
                            <field name="total_iou_requests" widget="statinfo"
                                string="IOU Requests" />
                        </button>
                        <button name="action_view_denominations" type="object"
                            class="oe_stat_button" icon="fa-calculator"
                            invisible="not current_denomination_id">
                            <div class="o_field_widget o_stat_info">
                                <span class="o_stat_value">
                                    <field name="current_denomination_id" readonly="1" />
                                </span>
                                <span class="o_stat_text">Denominations</span>
                            </div>
                        </button>
                        <button name="action_setup_denominations" type="object"
                            class="oe_stat_button" icon="fa-cog"
                            invisible="state != 'approved' or current_denomination_id">
                            <div class="o_field_widget o_stat_info">
                                <span class="o_stat_value text-warning">
                                    <i class="fa fa-exclamation-triangle"></i>
                                </span>
                                <span class="o_stat_text">Setup Required</span>
                            </div>
                        </button>
                        <!-- <button name="action_view_all_requests" type="object"
                            class="oe_stat_button" icon="fa-list">
                            <field name="total_requests" widget="statinfo"
                                string="Total Requests" />
                        </button> -->
                    </div>
                    <div class="alert alert-info" invisible="state != 'draft'">
                        <div class="d-flex align-items-center">
                            <i class="fa fa-info-circle me-2"></i>
                            <div>
                                <strong>Draft Float Request</strong>
                                <p class="mb-0">Fill in all required fields and submit for approval
                                    when ready.</p>
                            </div>
                        </div>
                    </div>

                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Float Name (e.g., Marketing Float)"
                                required="1" />
                        </h1>
                    </div>

                    <group>
                        <group>
                            <field name="department_id"
                                options="{'no_create': True}" />
                            <field name="initial_amount"
                                widget="monetary" />
                            <field name="current_amount"
                                widget="monetary"
                                readonly="1" />
                            <field name="cash_in_hand"
                                widget="monetary"
                                readonly="1" />
                        </group>
                        <group>
                            <field name="float_manager_id"
                                options="{'no_create': True, 'no_edit': True}" />
                            <field name="date_created" />
                            <field name="can_exceed" />
                            <field name="exceed_limit" widget="monetary"
                                invisible="not can_exceed" />
                        </group>
                    </group>

                    <group>
                        <field name="remarks" placeholder="Enter remarks (optional)" />
                    </group>

                    <group string="Attachment" invisible="not attachment">
                        <field name="attachment" filename="attachment_filename" />
                        <field name="attachment_filename" invisible="1" />
                    </group>

                    <group string="Upload Attachment" invisible="attachment">
                        <field name="attachment" filename="attachment_filename" />
                        <field name="attachment_filename" invisible="1" />
                    </group>

                    <group string="Financial Summary"
                        invisible="state not in ['approved', 'completed']">
                        <group>
                            <field name="initial_amount" string="Initial Float" widget="monetary"
                                readonly="1" />
                            <field name="current_amount" string="Current Balance" widget="monetary"
                                readonly="1" />
                        </group>
                        <group>
                            <field name="iou_amount" string="Pending IOUs" widget="monetary"
                                readonly="1" />
                            <field name="cash_in_hand" string="Available Cash" widget="monetary"
                                readonly="1" />
                        </group>

                    </group>
                    <group string="Denomination Count"
                        invisible="state != 'approved' or not current_denomination_id">
                        <div class="alert alert-info" invisible="current_denomination_id">
                            <div class="d-flex align-items-center">
                                <i class="fa fa-info-circle me-2"></i>
                                <div>
                                    <strong>Denomination Setup Required</strong>
                                    <p class="mb-0">Click "Setup Denominations" to configure the
                                        initial cash breakdown for this float.</p>
                                </div>
                            </div>
                        </div>

                        <field name="current_denomination_id" readonly="1"
                            invisible="not current_denomination_id" />
                        <field name="denomination_ids" mode="list" readonly="1"
                            invisible="not current_denomination_id">
                            <list decoration-success="total_amount > 0">
                                <field name="denom_5000_qty" string="Rs. 5,000" />
                                <field name="denom_1000_qty" string="Rs. 1,000" />
                                <field name="denom_500_qty" string="Rs. 500" />
                                <field name="denom_100_qty" string="Rs. 100" />
                                <field name="denom_50_qty" string="Rs. 50" />
                                <field name="denom_20_qty" string="Rs. 20" />
                                <field name="denom_10_qty" string="Rs. 10" />
                                <field name="denom_5_qty" string="Rs. 5" />
                                <field name="denom_2_qty" string="Rs. 2" />
                                <field name="denom_1_qty" string="Rs. 1" />
                                <field name="total_amount" widget="monetary" string="Total" />
                                <field name="last_updated" string="Last Updated" />
                            </list>
                        </field>
                    </group>
                </sheet>
            </form>
        </field>


    </record>

    <record id="view_search_float_request" model="ir.ui.view">
        <field name="name">float.request.search</field>
        <field name="model">float.request</field>
        <field name="arch" type="xml">
            <search string="Search Petty Cash Floats">
                <field name="name" />
                <field name="float_manager_id" />
                <field name="initial_amount" />

                <filter string="Draft" name="draft" domain="[('state', '=', 'draft')]" />
                <filter string="Requested" name="requested"
                    domain="[('state', '=', 'requested')]" />
                <filter string="Approved" name="approved" domain="[('state', '=', 'approved')]" />
                <filter string="Rejected" name="rejected" domain="[('state', '=', 'rejected')]" />

                <separator />
                <filter string="Can Exceed Limit" name="can_exceed"
                    domain="[('can_exceed', '=', True)]" />
                <filter string="My Requests" name="my_requests"
                    domain="[('create_uid', '=', uid)]" />
                <filter string="My Managed Floats" name="my_managed"
                    domain="[('float_manager_id', '=', uid)]" />

                <group expand="0" string="Group By">
                    <filter string="Status" name="group_status"
                        context="{'group_by': 'state'}" />
                    <filter string="Float Manager" name="group_manager"
                        context="{'group_by': 'float_manager_id'}" />
                    <filter string="Creation Date" name="group_date"
                        context="{'group_by': 'date_created'}" />
                </group>
            </search>
        </field>
    </record>


    <!-- Action -->
    <record id="action_float_request" model="ir.actions.act_window">
        <field name="name">Petty Cash Floats</field>
        <field name="res_model">float.request</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="context">{
            'search_default_my_requests': 1,
            'default_state': 'draft',
            }</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first float request!
            </p>
            <p> Floats are used to manage petty cash for different departments. <br />Click "Create"
                to set up a new petty cash float. </p>
        </field>
    </record>

    <record id="action_create_float_request" model="ir.actions.act_window">
        <field name="name">Create New Float</field>
        <field name="res_model">float.request</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="context">{'default_state': 'draft'}</field>
    </record>

</odoo>