<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Kanban View (Optional) -->
    <record id="view_kanban_float_request" model="ir.ui.view">
        <field name="name">float.request.kanban</field>
        <field name="model">float.request</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_horizontal_floats" create="false"
                edit="false"
                delete="false"
                quick_create="false">
                <field name="name" />
                <field name="initial_amount" />
                <field name="float_manager_id" />
                <field name="total_petty_cash_requests" />
                <field name="total_iou_requests" />
                <field name="date_created" />
                <field name="state" />
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card oe_kanban_global_click oe_kanban_no_click">
                            <div class="card-header">
                                <strong>
                                    <field name="name" />
                                </strong>
                                <span>
                                    <strong>Rs. <field name="initial_amount" /></strong>
                                </span>
                            </div>

                            <div class="float-card-buttons">
                                <button type="action" name="petty-cash.action_petty_cash_requests"
                                    class="btn btn-primary"
                                    context="{
                                    'search_default_float_request_id': id,
                                    'default_float_request_id': id,
                                }">
                                    Petty Cash Request
                                </button>
                                <button type="action" name="petty-cash.action_iou_requests"
                                    class="btn btn-primary"
                                    context="{
                                    'search_default_float_request_id': id,
                                    'default_float_request_id': id,
                                    }">
                                    IOU Request
                                </button>
                                <button class="btn btn-primary">Reimbursement</button>
                                <button class="btn btn-primary">Denomination Count</button>
                            </div>

                            <div class="float-card-summary">
                                <p>Cash in hand: Rs. <t
                                        t-esc="record.initial_amount.raw_value - record.total_iou_requests.raw_value" />
                                </p>
                                <p>Total IOU Requests: <field name="total_iou_requests" /></p>
                                <p>Total Petty Cash Requests: <field
                                        name="total_petty_cash_requests" /></p>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>


    <record id="view_tree_float_request" model="ir.ui.view">
        <field name="name">float.request.list</field>
        <field name="model">float.request</field>
        <field name="arch" type="xml">
            <list string="Petty Cash Floats">
                <field name="date_created" />
                <field name="name" />
                <field name="initial_amount" />
                <field name="state" widget="badge"
                    decoration-success="state == 'approved'"
                    decoration-warning="state == 'requested'"
                    decoration-info="state == 'draft'"
                    decoration-danger="state == 'rejected'" />

            </list>

        </field>

    </record>


    <record id="view_form_float_request" model="ir.ui.view">
        <field name="name">float.request.form</field>
        <field name="model">float.request</field>
        <field name="arch" type="xml">
            <form string="Create New Float" editable="top">
                <header>
                    <button name="action_submit" string="Submit for Approval"
                        type="object" class="btn-primary"
                        invisible="state != 'draft'" />
                    <!-- <button name="action_approve" string="Approve"
                        type="object" class="btn-success"
                        invisible="state != 'requested'"
                        groups="account.group_account_manager" />
                    <button name="action_reject" string="Reject"
                        type="object" class="btn-danger"
                        invisible="state != 'requested'"
                        groups="account.group_account_manager" /> -->
                    <field name="state" widget="statusbar"
                        statusbar_visible="draft,requested,approved" />
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Float Name" />
                        </h1>
                    </div>

                    <group>
                        <group>
                            <field name="initial_amount" />

                            <field name="can_exceed" />
                        </group>
                        <group>
                            <field name="float_manager_id"
                                options="{'no_create': True, 'no_edit': True}" />
                            <field name="date_created" />
                        </group>
                    </group>

                    <group>
                        <field name="remarks" placeholder="Enter remarks (optional)" />
                    </group>

                    <group string="Attachment" invisible="not attachment">
                        <field name="attachment" filename="attachment_filename" />
                        <field name="attachment_filename" invisible="1" />
                    </group>

                    <group string="Upload Attachment" invisible="attachment">
                        <field name="attachment" filename="attachment_filename" />
                        <field name="attachment_filename" invisible="1" />
                    </group>
                </sheet>
            </form>
        </field>


    </record>

    <record id="view_search_float_request" model="ir.ui.view">
        <field name="name">float.request.search</field>
        <field name="model">float.request</field>
        <field name="arch" type="xml">
            <search string="Search Petty Cash Floats">
                <field name="name" />
                <field name="float_manager_id" />
                <field name="initial_amount" />

                <filter string="Draft" name="draft" domain="[('state', '=', 'draft')]" />
                <filter string="Requested" name="requested"
                    domain="[('state', '=', 'requested')]" />
                <filter string="Approved" name="approved" domain="[('state', '=', 'approved')]" />
                <filter string="Rejected" name="rejected" domain="[('state', '=', 'rejected')]" />

                <separator />
                <filter string="Can Exceed Limit" name="can_exceed"
                    domain="[('can_exceed', '=', True)]" />
                <filter string="My Requests" name="my_requests"
                    domain="[('create_uid', '=', uid)]" />
                <filter string="My Managed Floats" name="my_managed"
                    domain="[('float_manager_id', '=', uid)]" />

                <group expand="0" string="Group By">
                    <filter string="Status" name="group_status"
                        context="{'group_by': 'state'}" />
                    <filter string="Float Manager" name="group_manager"
                        context="{'group_by': 'float_manager_id'}" />
                    <filter string="Creation Date" name="group_date"
                        context="{'group_by': 'date_created'}" />
                </group>
            </search>
        </field>
    </record>


    <!-- Action -->
    <record id="action_float_request" model="ir.actions.act_window">
        <field name="name">Petty Cash Floats</field>
        <field name="res_model">float.request</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="context">{'search_default_my_requests': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first float request!
            </p>
            <p>
                Click "Create" to request a new petty cash float.
            </p>
        </field>
    </record>


</odoo>